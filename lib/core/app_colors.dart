import 'package:flutter/material.dart';

//#8ae670, #a3ff88, #708be6, #8aa2ff, #f07a94
class AppColors {
  // ── Verdes principales ─────────────────────────────────
  static const Color verdePrincipal  = Color(0xFF8AE670);
  static const Color verdeClaro      = Color(0xFFA3FF88);
  static const Color verdeFondo      = Color(0xFFF0FFF0);

  // ── Azul / Morado ──────────────────────────────────────
  static const Color azulPrincipal   = Color(0xFF708BE6);
  static const Color azulClaro       = Color(0xFF8AA2FF);
  static const Color azulFondo       = Color(0xFFF5F0FF);

  // ── Acento ─────────────────────────────────────────────
  static const Color rosa            = Color(0xFFF07A94);

  // ── Niveles de la mascota ──────────────────────────────
  static const Color nivelSalud      = Color(0xFFE74C6F);
  static const Color nivelEnergia    = Color(0xFFFF8C42);
  static const Color nivelHambre     = Color(0xFFFFD166);
  static const Color nivelLimpieza   = Color(0xFF4FC3F7);
  static const Color nivelAfecto     = Color(0xFFF07A94);

  // ── Estados de la mascota ──────────────────────────────
  static const Color estadoFeliz       = Color(0xFF8AE670);
  static const Color estadoHambriento  = Color(0xFFFFD166);
  static const Color estadoTriste      = Color(0xFF708BE6);
  static const Color estadoEnfermo     = Color(0xFFE74C6F);

  // ── Neutros ────────────────────────────────────────────
  static const Color fondoPrincipal  = Color(0xFFF0FFF0);
  static const Color fondoBlanco     = Color(0xFFFFFFFF);
  static const Color textoPrincipal  = Color(0xFF1A2E1A);
  static const Color textoSecundario = Color(0xFF73726C);
  static const Color borde           = Color(0xFFD3D1C7);

  static const Color doradoSuave = Color(0xFFFFF4BF);
  static const Color doradoBorde = Color(0xFFF2D66B);
  static const Color doradoTexto = Color(0xFF6B5500);
}
